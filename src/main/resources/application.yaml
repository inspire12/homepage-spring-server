server:
  port: 8080

spring:
  banner:
    location: classpath:/META-INF/banner.txt
  jpa:
    database: mysql
    show-sql: true
    hibernate:
      ddl-auto: create-drop # update valid
      naming:
        implicit-strategy: org.springframework.boot.orm.jpa.hibernate.SpringImplicitNamingStrategy
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    generate-ddl: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect
    open-in-view: true
  datasource:
    url: jdbc:mysql://localhost:3306/homepage?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Seoul
    username: manager
    password: 1q2w3e4r!@
    driver-class-name: com.mysql.cj.jdbc.Driver
  mail:
    host: smtp.gmail.com
    port: 587
    username: seo.yeonghak.1992@gmail.com
    password: ENC(xShDDxLAewa+JYvmdwWuVsGHzC8oXr1h)
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            required: true
            enable: true

  devtools:
    livereload:
      enabled: true

  redis:
    lettuce:
      pool:
        max-active: 10
        max-idle: 10
        min-idle: 2
    host: 127.0.0.1
    port: 6379

servlet:
  multipart:
    max-file-size: 128MB
    max-request-size: 128MB


logging:
  level:
    web: INFO
    sql: INFO
    root: WARN
    tomcat: INFO

  file: logs/homepage.log
  path: logs/
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"
    file: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%"


social:
  kakao:
    client_id: 6e358349e45f9b78dd992396d4da0368

env:
  dev: local
